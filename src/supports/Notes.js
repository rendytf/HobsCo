// Reactstrap VS Bootstrap
    // Reactstrap ==> component siap pakai
    // Bootstrap ==> class siap pakai
    // Menggunakan reactsrap ?? karena JQuery di Bootstrap tidak di rekomendasikan di React

var money = 1000000
money = 'Rp. ' + money.toLocaleString('id-ID')

console.log(money)